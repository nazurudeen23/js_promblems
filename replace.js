/**
 * replace(regexp, newSubstr)
 replace(regexp, replacerFunction)

 *replace(substr, newSubstr)
 replace(substr, replacerFunction)

 *The replace() method returns a new string with some or all matches of a pattern replaced by a replacement value.

 */



let s = "the training"
function val (){
    s = s.replace("the"," ");
    console.log(s)
}

val()

